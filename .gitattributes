# Unified-diff context lines intentionally contain one space. Preserve the
# verified patch bytes on Windows; don't treat patch syntax as source whitespace.
native/patches/*.patch text eol=lf whitespace=-blank-at-eol,-blank-at-eof
